#ifndef LIBGM_MATH_TAGS_HPP
#define LIBGM_MATH_TAGS_HPP

namespace libgm {

  /**
   * A tag that is used to indicate a log-space value.
   * \ingroup math_number
   */
  struct log_tag { };

  /**
   * A tag that is used to indicate a probablity-space value.
   * \ingroup math_number
   */
  struct prob_tag { };

} // namespace libgm

#endif
