Want (all single-threaded):
- factors:
  tables, arrays/matrices, moment_gaussian, canonical_gaussian
- graph algorithms:
  triangulation (sill), elimination
- datastructures:
  junction tree formation (sill), restructuring
- models:
  decomposable model calibration
  naive bayes inference
- inference algorithms:
  exact: sum-product, belief update
  loopy: pairwise LBP (different versions), GBP
  variational: bipartite mean field
- learning:
  chow-liu (sill), mixture EM

Run this as a test suite (always release mode)
